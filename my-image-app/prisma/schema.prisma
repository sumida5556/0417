generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Image {
  id        String   @id @default(cuid())
  url       String
  filename  String
  createdAt DateTime @default(now())
}